# Переименование проекта: XiverCRM → XiverCMS

## ✅ Что было переименовано

### Backend (Go)
- ✅ Модуль в `go.mod`: `github.com/xivercrm/xivercrm` → `github.com/xivercms/xivercms`
- ✅ Все импорты в Go файлах обновлены
- ✅ Конфигурация БД: `xivercrm` → `xivercms`
- ✅ Email админа: `admin@xivercrm.com` → `admin@xivercms.com`

### Frontend
- ✅ `package.json`: `xivercrm-frontend` → `xivercms-frontend`
- ✅ Все компоненты Vue обновлены
- ✅ HTML файлы обновлены

### Docker
- ✅ `docker-compose.yml`: контейнеры переименованы
- ✅ Имена БД обновлены

### Документация
- ✅ Все `.md` файлы обновлены
- ✅ `README.md` обновлен
- ✅ `CHANGELOG.md` обновлен
- ✅ Docsify конфигурация обновлена

### Конфигурация
- ✅ `Makefile` обновлен (bin/xivercms)
- ✅ GitHub Actions workflow обновлен

## 📝 Что нужно сделать вручную

### 1. Переименовать корневой каталог

```bash
# Переименуйте каталог
mv /home/rodving/Документы/go/XiverCRM /home/rodving/Документы/go/XiverCMS

# Перейдите в новый каталог
cd /home/rodving/Документы/go/XiverCMS
```

### 2. Обновить репозиторий на GitHub

Если репозиторий уже создан:

1. **Переименовать репозиторий на GitHub:**
   - Settings → General → Repository name
   - Измените `xivercrm` на `xivercms`

2. **Обновить remote URL:**
```bash
git remote set-url origin https://github.com/YOUR_USERNAME/xivercms.git
```

### 3. Обновить зависимости

```bash
# Обновите Go модули
go mod tidy

# Проверьте импорты
go build ./...
```

### 4. Обновить переменные окружения

Если у вас есть `.env` файл, обновите:
```env
DB_NAME=xivercms
DB_PATH=./data/xivercms.db
```

### 5. Пересоздать базу данных (если нужно)

Если база уже создана со старым именем:
```bash
# Удалите старую БД
rm -rf data/xivercrm.db

# Перезапустите приложение (БД создастся автоматически)
make run
```

## ✅ Проверка

После переименования проверьте:

```bash
# Проверьте, что нет упоминаний старого имени
grep -ri "xivercrm" . --exclude-dir=.git

# Проверьте сборку
go build ./...

# Проверьте, что все импорты корректны
go mod verify
```

## 🎯 Причина переименования

- **XiverCRM** - неправильное название (CRM = Customer Relationship Management)
- **XiverCMS** - правильное название (CMS = Content Management System)
- Проект функционально является CMS, а не CRM

