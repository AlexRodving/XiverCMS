# XiverCRM - Headless CMS

XiverCRM - это современная headless CMS система, построенная на Go (Gin + GORM) и Vue3. Она предоставляет гибкую систему управления контентом, похожую на Strapi, но с использованием Go для backend.

## Особенности

- 🚀 **Высокая производительность** - Go backend обеспечивает отличную скорость работы
- 📦 **Динамические Content Types** - создавайте любые типы контента без изменения кода
- 🎨 **Визуальный редактор полей** - удобный редактор для создания схем контента (как в Strapi)
- 🔗 **Связи между Content Types** - поддерживаются все типы связей (oneToOne, oneToMany, manyToOne, manyToMany)
- 👥 **Управление пользователями и ролями** - гибкая система прав доступа (RBAC)
- 🔐 **JWT аутентификация** - безопасная авторизация
- 🎫 **API Tokens** - токены для программного доступа к API
- 📸 **Media Library** - управление медиа файлами (загрузка, хранение, удаление)
- 📝 **Audit Logs** - логирование всех действий пользователей
- 📚 **Content History** - история изменений контента
- 🔒 **Role-Based Access Control (RBAC)** - детальное управление правами доступа
- 📱 **Современный UI** - красивая админ-панель на Vue3 с Tailwind CSS
- 🗄️ **Гибкая база данных** - поддержка PostgreSQL и SQLite
- 🔌 **RESTful API** - готовый API для интеграции с любыми фронтенд-приложениями

## Технологии

### Backend
- Go 1.21+
- Gin (HTTP web framework)
- GORM (ORM)
- JWT для аутентификации
- PostgreSQL / SQLite

### Frontend
- Vue 3
- Vue Router
- Pinia (state management)
- Tailwind CSS
- Vite
- Axios

## Быстрый старт

### Предварительные требования

- Go 1.21 или выше
- Node.js 18+ и npm/yarn
- PostgreSQL (опционально, по умолчанию используется SQLite)

### Установка Backend

1. Клонируйте репозиторий:
```bash
cd XiverCRM
```

2. Установите зависимости:
```bash
go mod download
```

3. Создайте файл `.env` (см. `.env.example`):
```bash
cp .env.example .env
# Отредактируйте .env при необходимости
```

4. Запустите сервер:
```bash
go run main.go
```

Backend будет доступен по адресу `http://localhost:8080`

### Установка Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev-сервер:
```bash
npm run dev
```

Frontend будет доступен по адресу `http://localhost:5173`

### Дефолтные учетные данные

После первого запуска создается администратор:
- Email: `admin@xivercrm.com`
- Password: `admin123`

**⚠️ ВАЖНО:** Измените пароль администратора в production!

## Структура проекта

```
XiverCRM/
├── config/          # Конфигурация приложения
├── models/          # GORM модели
├── database/         # Настройка БД и миграции
├── handlers/         # HTTP handlers
├── middleware/       # Middleware (auth, CORS)
├── routes/           # Маршруты API
├── auth/             # Аутентификация и JWT
├── main.go           # Точка входа
├── frontend/         # Vue3 фронтенд
│   ├── src/
│   │   ├── api/      # API клиенты
│   │   ├── views/    # Страницы
│   │   ├── layouts/  # Layout компоненты
│   │   ├── stores/   # Pinia stores
│   │   └── router/   # Vue Router
│   └── package.json
└── README.md
```

## API Endpoints

### Authentication
- `POST /api/auth/login` - Вход
- `POST /api/auth/register` - Регистрация
- `GET /api/auth/me` - Получить текущего пользователя

### Users
- `GET /api/users` - Список пользователей
- `GET /api/users/:id` - Получить пользователя
- `PUT /api/users/:id` - Обновить пользователя
- `DELETE /api/users/:id` - Удалить пользователя

### Content Types
- `GET /api/content-types` - Список типов контента
- `GET /api/content-types/:uid` - Получить тип контента
- `POST /api/content-types` - Создать тип контента
- `PUT /api/content-types/:uid` - Обновить тип контента
- `DELETE /api/content-types/:uid` - Удалить тип контента

### Content Entries
- `GET /api/content-types/:uid/entries` - Список записей
- `GET /api/content-types/:uid/entries/:id` - Получить запись
- `POST /api/content-types/:uid/entries` - Создать запись
- `PUT /api/content-types/:uid/entries/:id` - Обновить запись
- `DELETE /api/content-types/:uid/entries/:id` - Удалить запись
- `GET /api/content-types/:uid/entries/:id/history` - История изменений записи

### Media Library
- `POST /api/upload` - Загрузить файл
- `GET /api/media-files` - Список медиа файлов
- `GET /api/media-files/:id` - Получить медиа файл
- `PUT /api/media-files/:id` - Обновить медиа файл
- `DELETE /api/media-files/:id` - Удалить медиа файл

### Roles & Permissions
- `GET /api/roles` - Список ролей
- `GET /api/roles/:id` - Получить роль
- `POST /api/roles` - Создать роль
- `PUT /api/roles/:id` - Обновить роль
- `DELETE /api/roles/:id` - Удалить роль
- `GET /api/permissions` - Список прав доступа
- `GET /api/permissions/:id` - Получить право доступа
- `POST /api/permissions` - Создать право доступа
- `PUT /api/permissions/:id` - Обновить право доступа
- `DELETE /api/permissions/:id` - Удалить право доступа

### API Tokens
- `GET /api/api-tokens` - Список API токенов
- `GET /api/api-tokens/:id` - Получить API токен
- `POST /api/api-tokens` - Создать API токен
- `PUT /api/api-tokens/:id` - Обновить API токен
- `DELETE /api/api-tokens/:id` - Удалить API токен

### Audit Logs
- `GET /api/audit-logs` - Список логов аудита

## Пример использования

### Создание Content Type

```bash
curl -X POST http://localhost:8080/api/content-types \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "uid": "article",
    "displayName": "Article",
    "kind": "collectionType",
    "schema": {
      "title": {"type": "string"},
      "content": {"type": "text"},
      "published": {"type": "boolean"}
    }
  }'
```

### Создание записи

```bash
curl -X POST http://localhost:8080/api/content-types/article/entries \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "data": {
      "title": "My First Article",
      "content": "This is the content",
      "published": true
    },
    "status": "published"
  }'
```

## Разработка

### Запуск в режиме разработки

Backend:
```bash
GIN_MODE=debug go run main.go
```

Frontend:
```bash
cd frontend && npm run dev
```

### Сборка для production

Backend:
```bash
go build -o xivercrm main.go
./xivercrm
```

Frontend:
```bash
cd frontend && npm run build
```

## 📚 Документация

Полная документация доступна на [GitHub Pages](https://your-username.github.io/xivercrm/) или локально:

```bash
cd docs
docsify serve
```

Документация включает:
- Подробное руководство по установке
- Полная API документация
- Примеры использования
- Руководство по разработке
- Deployment инструкции

## Лицензия

MIT

## Автор

XiverCRM Team

